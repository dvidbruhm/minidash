services:
  minidash:
    build: .
    image: minidash:latest
    container_name: minidash
    ports:
      - "8080:8080"
    volumes:
      - ./config:/config
    environment:
      MINIDASH_PASSWORD: "changeme"   # remove to use password_hash in config.yaml instead
    restart: unless-stopped
